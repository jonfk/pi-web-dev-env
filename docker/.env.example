# Copy this file to docker/.env and adjust values for your machine.
#
# The SSH key is mounted read-only into the pipane and wede containers as:
#   /home/ubuntu/.ssh/id_ed25519
PI_SSH_KEY_PATH=${HOME}/.ssh/id_ed25519

# Optional: mount a different project into /workspace.
# WORKSPACE_DIR=..
