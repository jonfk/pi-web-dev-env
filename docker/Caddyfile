http://{$PIPANE_HOSTNAME:pipane.localhost}:{$APP_PORT:8080} {
	reverse_proxy pipane:8222
}

http://{$WEDE_HOSTNAME:wede.localhost}:{$APP_PORT:8080} {
	reverse_proxy wede:9090
}

http://localhost:{$APP_PORT:8080} {
	redir http://{$PIPANE_HOSTNAME:pipane.localhost}:{$APP_PORT:8080}
}
